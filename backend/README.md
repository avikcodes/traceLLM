# TraceLLM Backend Package

Install in editable mode with:

```bash
pip install -e .
```

This package exposes:

- `from tracellm import trace`
- `tracellm demo`
- `tracellm trace`
- `tracellm replay`
